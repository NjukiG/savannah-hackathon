import React from 'react'

const VerifyStartVisitToken = (clientToken, startVisitToken) => {
    console.log(clientToken, startVisitToken);
  return (
    <div>VerifyStartVisitToken</div>
  )
}

export default VerifyStartVisitToken